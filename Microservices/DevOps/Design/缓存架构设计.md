# 缓存架构设计

# 作用

高并发&高性能

# 分类

## 实时性高的数据

实时的缓存+数据库双写的技术方案，双写一致性保证的方案

## 实时性不高的数据

三级缓存架构的技术方案

# 架构

## 数据量不大（10G以内）

Redis Singleton

Redis持久化+备份方案+容灾方案+replication（主从+读写分离）

## 数据量很大（1T+）

Redis Cluster

多master分布式存储数据，水平扩容，自动进行master→slave的主备切换

[[../../分布式中间件/分布式缓存（Distributed Cache）/Redis/缓存和数据库双写一致性]]

## 大型缓存全量更新问题对的解决方案

缓存纬度化

多级缓存

## 分布式并发缓存重建的冲突问题的解决方案

分布式锁

## 缓存冷启动的问题的解决方案

缓存预热

## 恐怖的缓存雪崩问题的解决方案

事前：好可用构架。主从构架，操作主节点，读写，数据同步到从节点，一旦主节点挂掉，从节点更上

事中：多级缓存。Redis Cluster已经彻底崩溃了，缓存服务实例的ecache的缓存还能起作用

事后：Redis数据可以恢复，做了备份，Redis数据备份和恢复，Redis重新启动起来

## ORM缓存的基本概念

1. 以减少数据库服务器磁盘IO为最终目的，而不是减少发送到数据库的SQL条数。实际上使用ORM，会显著增加SQL条数，有时候会成倍增加SQL。
2. 数据库schema设计的取向是尽量设置 细颗粒度的表，表和表之间用外键关联，颗粒度越细，缓存对象的单位越小，缓存的应用场景越广泛
3. 尽量避免夺标关联查询，尽量拆陈多个表单独的主键查询，尽量多制造n+1条查询，不要害怕“臭名昭著”的n+1问题，实际上n+1才能有效利用ORM缓存

### 按照列进行拆表

1. 将大字段拆分出来，放在一个独立的表里面，表只有主键和大字段，外键放在主表当中
2. 将不参与where条件的统计查询的字段拆分出来，放在独立的表中，外键放在主表中

按照行拆表本质上是一个去关系化的过程。主表只保留参与关系运算的字段，将会关系型的字段剥离到关联表当中，关联表仅允许组建查询，以Key-Value DB的方式来访问。因此这种缓存设计模式本质上是一种SQLDB和NoSQLDB的混合架构设计

# 附录

[缓存方案如何设计才能切合业务需求？](https://www.zhihu.com/question/37747712)