---
title: 存储1
date: 2026-04-07
tags: [基础设施, 数据库]
type: note
status: incomplete
---

# 存储1

```jsx
CREATE DEFINER=`meddev`@`%` PROCEDURE `NewProc`()
BEGIN
  DECLARE num INT;
	DECLARE diff INT;
	SET @diff = DATEDIFF('2020-01-01', '2019-01-01');
	SET @num = 0;
	WHILE @num < @diff DO	
		INSERT INTO `med_d2d`.`patient_ext_build_records`(`patient_id`, `type`, `data`, `created_at`, `updated_at`) VALUES(78334710,2,'16',DATE_ADD('2019-01-01',INTERVAL @num DAY),DATE_ADD('2019-01-01',INTERVAL @num DAY)) ;
		SET @num = @num + 1 ;
	END WHILE;
END
```