---
title: 哈希分区规则
date: 2026-04-07
tags:
  - 算法
  - 奇门遁甲
type: guide
status: complete
---

## 哈希分区规则

### 前沿

### 哈希算法

将任意长度的二进制字符串映射为定长二进制字符串的映射规则我们称为散列（hash）算法，又叫哈希（hash）算法，而通过原始数据映射之后得到的二进制值称为哈希值。

### 常用哈希算法

- MED5
- SHA-1

### 哈希取模算法（取余法）

### 具体操作

1. 关键字段Hash计算
2. Hash之后的值取模

### 问题

1. 增减节点时如何保证对现有数据做最小的移动
2. 某个节点down掉之后保存在该节点上的数据不能使用

### 一致性哈希算法

一致性的Hash算法是对2的32方取模。即，一致性Hash算法将整个Hash空间组织成一个虚拟的圆环，Hash函数的值空间为0 ~ 2^32 - 1(一个32位无符号整型)，

具体在计算一致性hash时采用如下步骤：

1. 首先求出服务器（节点）的哈希值，并将其配置到0～2的圆（continuum）上。
2. 然后采用同样的方法求出存储数据的键的哈希值，并映射到相同的圆上。
3. 然后从数据映射到的位置开始顺时针查找，将数据保存到找到的第一个服务器上。如果超过2仍然找不到服务器，就会保存到第一台服务器上。

### 哈希环

节点增减（节点down）

### 虚拟节点

数据倾斜问题

数据迁移问题

### 虚拟槽