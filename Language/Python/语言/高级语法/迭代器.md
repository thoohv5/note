---
title: 迭代器
date: 2026-04-07
  - 编程语言
  - Python
type: note
status: complete
---

## 迭代器

“迭代”是指逐个地遍历容器对象的每个元素。例如，对于 str、list、tuple 等容器类的对象。

可以用 `isinstance()`函数来测试对象是否为可迭代的(`iterable`)。

对象是可迭代的(`iterable`) 是指它所属的类定义了一个可以返回一个迭代器(**iterator**)的**`__iter__**()`方法，该对象称为可迭代对象。

对象是一个迭代器(`iterator`) 是指它所属的类定义了**`__next__**()`方法，该方法要么返回可迭代对象的下一个元素，要么抛出(raise)一个 StopIteration 异常。

```python
class Iterable():
    def __init__(self, *items):
        self.i = 0
        self.__items = items
    def __iter__(self):
       return self
    def __next__(self):
        if self.i == len(self.__items):
            raise StopIteration()
        self.i += 1
        return self.__items[self.i - 1]

i = Iterable(*['a', 'b', 'c', 'd'])
for item in i:
    print(item, end=',')
```

对象是可迭代的，除其所属类实现`__iter__()`方法外，其所属类还可实现`__getitem__()`方法。

对象是可迭代的，除其所属类实现`__iter__()`方法外，其所属类还可实现`__**setitem**()__`方法。

```python
class Iterable():
    def __init__(self):
        self.i = 0
        self.__dict = {}
    def __getitem__(self, key):
        return self.__dict[key]
    def __setitem__(self, key, value):
        self.__dict[key] = value
    def __len__(self):
        return len(self.__dict)

i = Iterable()
i['1'] = 1
i['2'] = 2
print(i['1'])
```