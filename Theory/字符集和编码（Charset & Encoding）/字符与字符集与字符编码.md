---
title: 字符&字符集&字符编码
date: 2026-04-07
tags:
  - 理论
  - 字符集和编码
type: reference
status: complete
---

## 字符&字符集&字符编码

type: Post
status: Published
date: 2025/02/26
slug: 字符&字符集&字符编码
summary: 字符是文字和符号的总称，字符集是字符的集合，字符编码则是将字符映射为计算机可存储和传输的格式。常见字符集包括ASCII、GB2312、GBK、GB18030、BIG5和Unicode，UTF-8和UTF-16是常用的编码方式。BOM标记影响编码解码的兼容性。
tags: 理论
category: Theory

## 概述

字符（Character）是各种文字和符号的总称，包括各国家文字、标点符号、图形符号、数字等。

字符集（Character set）是多个**字符**的集合，字符集种类较多，每个字符集包含的字符个数不同，常见字符集名称：ASCII字符集、GB2312字符集、BIG5字符集、 GB18030字符集、Unicode字符集等。

字符编码（Character Encoding）是把**字符集**中的字符为指定集合中某一对象（例如：[比特](https://zh.wikipedia.org/wiki/位)83模式、[自然数](https://zh.wikipedia.org/wiki/自然)B0[序列](https://zh.wikipedia.org/wiki/序)97、[八比特](https://zh.wikipedia.org/wiki/八位)83或者[电脉冲](https://zh.wikipedia.org/wiki/電磁)A2），以便[文本](https://zh.wikipedia.org/wiki/文)AC在[计算机](https://zh.wikipedia.org/wiki/电子计算)BA中存储和通过[通信](https://zh.wikipedia.org/wiki/通)A1网络的传递。

字符集中的字符都是指唯一存在的抽象字符，而忽略了它的具体表现形式。在给定一个字符集合中的每个字符都分配了一个整数编号之后，这个字符集就有了顺序，就成为了**编码字符集**。同时，这个编号，可以唯一确定到底指的是哪一个字符

**编码字符集**是被分配了整数编号的字符集合，但是编码字符集中字符被分配的整数编号，不一定就是该字符在计算机中存储时所使用的值，计算机中存储的字符到底使用什么二进制整数值来表示，由**字符编码**决定。

## 常见字符集

### **ASCII字符集**

American Standard Code for Information Interchange，美国信息交换标准代码

ASCII 码使用指定的7 位或8 位二进制数组合来表示128 或256 种可能的字符。标准ASCII 码也叫基础ASCII码，使用7 位二进制数（剩下的1位二进制为0）来表示所有的大写和小写字母，数字0 到9、标点符号， 以及在美式英语中使用的特殊控制字符

**范围**

**0x00~0x7F（0-127）**

Go语言中，ASCII码0-32及127对应的字符为[**控制字符](https://zhida.zhihu.com/search?content_id=165582346&content_type=Article&match_order=1&q=控制字)A6&zd_token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJ6aGlkYV9zZXJ2ZXIiLCJleHAiOjE3NDUwNTE1MzcsInEiOiLmjqfliLblrZfnrKYiLCJ6aGlkYV9zb3VyY2UiOiJlbnRpdHkiLCJjb250ZW50X2lkIjoxNjU1ODIzNDYsImNvbnRlbnRfdHlwZSI6IkFydGljbGUiLCJtYXRjaF9vcmRlciI6MSwiemRfdG9rZW4iOm51bGx9.p11tiosTlrp4GS_I5dLSd0plUT4vubWKpyliz40ZDBk&zhida_source=entity（不可打印字符）**，其他能显示出来的就是**打印字符**。

### GB2312字符集和编码

GB2312总共覆盖了6763个常用汉字，**GB2312标准把ASCII码表127号之后的扩展字符集去掉，并规定，小于127（0x7F）的编码按照ASCII标准进行解码，当出现连续两个大于127（0x7F）的编码时，这两个连续的大于0x7F的编码表示一个汉字，第一二个字节都是用0xA1~0xFE进行编码**。其中，ASCII码中原有的数字字符、英文字符、标点等称为半角字符，大于0x7F的相应字符编码称为全角字符。

GB2312解码规则：当使用GB2312编码标准时，给定一串字符编码，按照字节进行检测，首先检测每个字节的大小，如果字节值小于0x7F，就用ASCII标准解码，如果连续两个字节的值都大于0x7F，就把这两个字节视为一个整体，使用GB2312标准解码。

GB2312 对汉字进行了分区处理，每个区含有 94 个汉字或者字符，总共有 94 个区，每个汉字或者字符都对应一个 分区编号和分区内的位置编号，称为 区位码

国标码 也叫 交换码，用于交换文件所使用的编码，在早期，不同的操作系统可能使用不同的内码，如果它们之间要交换文件，则会发生乱码的现象，当时的解决方法是交换文件之前先转成交换码再交换，接收者收到之后再转成内码

交换码是比较早期的一种方案，目前系统大都采用内码作为交换码

国标码 和 ASICII 码还是存在一定的重复，比如 "中" 字 的国标码是 86 80，对应第一个字节是 86，第二个字节是 80，而在 ASICII 码中它们分别代表大写字母V 和 大写字母 P，这就无法区分它们到底是一个汉字，还是两个字母。为了解决这一点，把国标码中的每个字节的最高位置为1，也即相当于每个字节都加上 128 ( 2的7次方 )，还是以 "中" 字为例，它的 国标码是 86 80，加上 128 后， 第一个字节是 86 + 128 = 214， 第二个字节是 80 + 128 = 208，转化成 16 进制是 0xD6 0xD0 ( 214 的十六进制是 0xD6， 208 的十六进制是 0xD0 )

国标码的每个字节都加上 128 后，得到国标码的机内码，简称 内码，汉字是以内码的形式在计算机中存储和传播的

![IMG_6097.png](../Theory/字符集和编码（Charset & Encoding)89/IMG_6097.png

### GBK字符集和编码

Chinese Internal Code Specification，汉字内码扩展规范

GBK编码在GB2312的基础上又增加了14240个汉字、生僻字和符号。按照GB2312的编码方式，两个字节已经不够用了，这时，**GBK编码制定了新的标准：只要出现一个大于0x7F的字节，那么这个字节和它后面一个字节共两个字节就表示一个汉字（GB2312规定两个字节都大于0x7F才表示一个汉字），这样做的好处就是，GBK编码兼容了ASCII编码和GB2312编码。**

GBK解码规则：当使用GBK编码标准时，给定一串字符编码，按照字节进行检测，首先检测每个字节的大小，如果字节值小于0x7F，就用ASCII标准解码，如果遇到一个大于0x7F的字节，就把该字节和它后面一个字节连在一起用GBK标准进行解码，然后从第三个字节开始继续遍历检测。

![IMG_6098.png](../Theory/字符集和编码（Charset & Encoding)89/IMG_6098.png

上述表格中，红色栏是 GBK 中包含的 GB2312 以及 ASICII 的编码范围，它们的编码范围保持不变；绿色栏的是 GBK 新增的编码范围；紫色栏是 用户自定义编码范围

### GB18030字符集和编码

变长多字节字符，使用1，2，4字节编码，兼容ASCII、GB2312、GBK。包括2000年编制的GB18030-2000，2005年编制的GB18030-2005

![IMG_6099.png](../Theory/字符集和编码（Charset & Encoding)89/IMG_6099.png

### BIG5字符集和编码

多用于台湾香港等地，主要是收录了繁体字。在包含汉字数量上来说，Big5是GBK的子集，但是二者的编码方式是不同的，比如同样“啊”，GBK编码为0xB0A1，Big5编码为0xB0DA。

### Unicode字符集

通用字符集（Universal Multiple-Octet Coded Character Set，USC）
UCS-2用两个字节编码，UCS-4用4个字节编码

**范围**

0x0000~0x007F

Unicode大端模式为 0xFE 0xFF

Unicode小端模式为 0xFF 0xFE

### UTF8编码

Unicode Transformation Format，Unicode字符集转换格式

Unicode Transformation Format 8，用1，2，3，4个字节对Unicode字符集进行编码，每个字符根据自己的编号范围进行相应编码。它的编码规则是这样的：

对于UTF-8单字节的编码，该字节最高位设为0，剩余位填入字符的Unicode编号，对于Unicode编号在0x00000000~0x0000007F的字符，UTF-8编码只要一个字节，兼容ASCII编码。对于N字节的编码，第一字节最高位开始，前N位置为1，第N+1位设0，剩余字节最高位设为10，这N个字节的其余空位填充该字符的Unicode编号，高位补0。具体可见下表：

![](https://img2023.cnblogs.com/blog/1090011/202307/1090011-20230710113015978-632220567.png)

解码时，看第一个字节

0开头：单字节解码；

110开头：双字节解码；

1110开头：三字节解码；

11110开头：四字节解码；

**UTF-8编码的时候，汉字一般是占三个字节的。**

### UTF16编码

### UTF32编码

## 其他

### BOM

Byte Order Mark，我们在Notepad++中的Encoding选项中可以看到诸如Encoding in UTF-8，以及Encoding in UTF-8-BOM这样的选项，带不带标签不会影响对字符的编码解码，假如把不带标签的UTF-8编码转换为带标签的UTF-8-BOM编码，程序和中文注释都不会出现乱码，但是这两种是有区别的。比如我们常用的emWin，在emWin中文支持中，它只支持不带标签的UTF-8编码，如果使用带标签的UTF-8-BOM，虽然不会报错，但是在控件中，无法显示这些中文。

ASICII、GB2312、GBK、GB18030 以及 UTF8 的关系

![IMG_6100.png](../Theory/字符集和编码（Charset & Encoding)89/IMG_6100.png

对于大部分中文字符来说，采用 GB18030 编码的话，只需两个字节，如果采用 UTF8 编码，就需要三个字节， 所以用 GB18030 存储和传输更节省空间

## 附录

[编码字符集和字符集编码傻傻分不清楚！看完这篇文章你就懂了？_charset和characterencoding的区别-CSDN博客](https://blog.csdn.net/chuixue24/article/details/130348165)

[【字符编码详解】ASCII、GB2312、GBK、UTF-8、UTF-16编码与Unicode字符集 - HelloMarsMan - 博客园](https://www.cnblogs.com/ToTigerMountain/articles/17540514.html)