# 导入导出

# 导出

```bash
# 导出
.output /tmp/$csv
.separator ","
select * from tableName;
.output stdout

# 备份
.output /tmp/$tableName
.dump $tableName
.output stdout

sqlite xxx/$test ".dump $tableName" > /tmp/$tableName

sqlite3 -csv ipam.db ".dump usablenetwork" > /tmp/usablenetwork
```

# 导入

```bash
# 还原
DROP TABLE database_name.table_name;
sqlite3 testDB.db < testDB.sql
```