# 余欣欣

PHP:

php-fpm 不太了解，SPL 不太了解，设计模式 一般了解，没有实操，索引数组和关联数组 不太了解，php trait 多继承 

MySQL:

优化场景：1. join 2. 索引失效，字段类型/最左原则/like/Null 3.where

InnoDB/事务隔离级别 不太了解 MySQL不了解原理

Redis:

AOF&RDB 不太了解，KEYS 问题不知道SCAN，Redis淘汰策略 不知道

与MySQL的一致性，不太了解

与Memchache区别 不太了解

分布式一致性：

zk 选举机制 不太了解