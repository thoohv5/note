---
title: 认证模块
date: 2026-04-07
tags: [基础设施, Nginx]
type: reference
status: complete
---

# 认证模块

## **ngx_http_auth_basic_module**

[Module ngx_http_auth_basic_module](http://nginx.org/en/docs/http/ngx_http_auth_basic_module.html)

```bash
location / {
    auth_basic           "closed site";
    auth_basic_user_file conf/.htpasswd;
}
```

### **htpasswd**

```bash
#Ubuntu
apt install apache2-utils -y
#CentOS
yum install httpd-tools -y

htpasswd -bc conf/htpasswd 'username' 'password'
```

### openssl

```bash
openssl passwd -crypt 'password'
```

## **ngx_http_auth_jwt_module**

[Module ngx_http_auth_jwt_module](http://nginx.org/en/docs/http/ngx_http_auth_jwt_module.html)

[commercial subscription](http://nginx.com/products/)

## **ngx_http_auth_request_module**

[Module ngx_http_auth_request_module](http://nginx.org/en/docs/http/ngx_http_auth_request_module.html)

作用：是通过子请求的响应状态码来实现客户端授权

**原理：收到请求后，先将该请求hold住，生成子请求，子请求和该请求内容是相同的。通过反向代理技术把子请求传递给上游服务，根据上游服务的响应再来决定是否处理当前的请求。**

功能：向上游的服务器转发请求，若上游服务器返回的响应码是2XX，则继续执行。若上游服务器返回的是401或者403，则将响应返回给客户端。

```bash
location /private/ {
      auth_request /auth;
}

location /auth {
			# 是否允许此路由可访问
			internal;
      proxy_set_header Host $host;
	    proxy_set_header X-Original-URI $request_uri;
	    proxy_set_header X-Original-METHOD $request_method;
	    proxy_pass_request_body off;
	    proxy_set_header Content-Length "";
	    proxy_pass http://localhost:8080/edu-online/api/auth;
}
```

测试

```bash
location / {
    root   html;
    index  index.html index.htm;
    auth_request /auth;
}

location /auth {
        #return 403 "error";
        return 200 "success";
}
```

# 附录

[ngx_http_auth_jwt_module](https://cloud.tencent.com/developer/section/1258990)

[Nginx 配置文件简介-2（Nginx 常用模块）](https://www.cnblogs.com/bushaoxun/p/9829964.html)