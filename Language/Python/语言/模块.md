---
title: 模块
date: 2026-04-07
tags: [编程语言, Python]
type: note
status: complete
---

# 模块

Python 模块(mudule)就是包含 Python 语句的、文件名后缀(文件扩展名)[是.py](http://xn--0iv.py/) 的文件。这个文 件中可以包含变量、函数和类等定义。

## 导入(import)模块

在程序中如果需要使用其他模块中的函数等功能，就需要用关键字 `import` 导入相应的模块。导 入模块的一般格式是:

```python
import 模块名
```

导入的模块只要说明模块名即可，不能有文件扩展名.py。程序代码中如果要使用模块中的对象，如函数，则需要用**.**运算符，即使用“模块名.函数名”访问具体的函数。

```python
import math
print(math.pi) # 3.141592653589793
```

### 重命名导入模块

可以用`import ...as` 对一个导入进来的模块重新命名。

```python
import math as m
print(m.pi) # 3.141592653589793
```

### 导入单独名字

可以用`from...import`从一个模块导入一个单独的名字，导入的这个名字就不用在前面添加“模 块名.”前缀了。

```python
from math import sqrt
print(sqrt(2)) # 1.4142135623730951
```

### 导入所有名字

还可以通过**`from**...**import ***`导入模块中的所有名字，该模块中的名字就可以直接使用，而不再需要模块名前缀了。

```python
from math import *
print(pi) # 3.141592653589793
print(sin(1.57)) # 0.9999996829318346
print(sqrt(2)) # 1.4142135623730951
```

## 函数`dir()`

如果没有指定可选的参数对象 `object`，则返回当前作用域中的所有名字，否则就返回指定的参 数对象的 `object` 的所有属性。

## 模块的`__name__`属性

每个模块都有一个`__name__`属性，当这个模块被其他程序(模块)通过 `import` 方式导入时，其值 就是这个模块的模块名。