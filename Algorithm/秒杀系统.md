# 秒杀系统

秒杀技术难点

- 超时高并发、负载压力大
- 读多写少
- 竞争资源是有限的、不能多卖、不能少卖、不能重卖

## 一、Redis 原子扣减库存 + 异步 MQ 消息落库 + 幂等保障 + 乐观锁 / 版本控制

### 实现思路

- 在系统初始化时，将商品的库存数量加载到redis缓存中
- 接收到秒杀请求时，在redis中进行预减库存（利用redis decr的原子性），当redis中的库存不足时，直接返回秒杀失败，否则继续进行第3步；
- 将请求放入异步队列中，返回正在排队中；
- 服务端异步队列将请求出队（哪些请求可以出队，可以根据业务来判定，比如：判断对应用户是否已经秒杀过对应商品，防止重复秒杀），出队成功的请求可以生成秒杀订单，减少数据库库存

## 二、其他方案

1. 数据库库存分段

## 补充

```bash
UPDATE stock_table SET stock = stock-num WHERE id = 商品ID AND stock-num >= 0;

# 版本可以维护在Redis中
SELECT version FROM stock_table WHERE id = 1;
UPDATE stock_table SET stock = stock - num, version = version + 1 WHERE id = 1 AND stock - num >= 0 AND version = ?;
```

# 附录

[juejin.cn](https://juejin.cn/post/6844904000337379341?utm_source=chatgpt.com)