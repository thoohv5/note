---
title: 事物隔离级别
date: 2026-04-07
tags: [基础设施, 数据库]
type: reference
status: complete
---

# 事物隔离级别

type: Post
status: Published
date: 2025/02/26
slug: 事物隔离级别
summary: 事务隔离级别包括原子性、一致性、隔离性和持久性，主要有未提交可读、提交可读、可重复读和可串行化四种类型。并发问题如脏读、不可重复读和幻读影响事务的正确性，InnoDB通过MVVC和next-key locks解决幻读问题。
tags: MySQL, 理论
category: Assembly

# 概述

事物隔离级别（Transaction Isolation Levels）

### **原子性(atomicity)**

事物开始后所有操作，要么全部做完，要么全部不做，不可能停滞在中间环节。事物执行过程中出错，会回滚到事物开始前的状态，所有的操作就像没有发生一样。也就是说事物是一个不可分割的整体，就像化学中学过的原子，是物质构成的基本单位。

### **一致性(consistency)**

事物开始前和结束后，数据库的完整性约束没有被破坏

### **隔离性(isolation)**

同一时间，只允许一个事物请求同一数据，不同的事物之间彼此没有任何干扰

### **持久性(durability)**

事物完成后，事物对数据库的所有更新将被保存到数据库，不能回滚

# 隔离级别

## 级别分类

### **未提交可读（READ UNCOMMITTED，RU）**

事务中的修改，即使没有提交，对其他事务也都是可见的。

### **提交可读（READ COMMITTED，RC）**

一个事务从开始时，只能“看见”已经提交的事务所做的修改。

### **可重复读（REPEATABLE READ，RR）**

Consistent reads within the same transaction read the snapshot established by the first read

一致读取，在同一事务中读取数据是第一次读取数据建立的快照

### **可串行化（SERIALIZABLE，S）**

强制事务串行执行。

## 并发问题

[](https://note.youdao.com/yws/api/personal/file/70BAE4CF8BC7464FA5302E98387DB548?method=download&shareKey=492d301c145d4d2a640f44f3acbc66e7)

### 脏读（Dirty Read）

当前事务读取了另一个事务已更新但尚未提交的数据。后果就是另一个事务回滚了，此数据就为错误的无效数据

### 不可重复读（Non-Repeatable Read）

当前事务读取相同数据2次的过程期间，另一个事务**修改**了此数据并提交，造成当前事务读取的2次结果不一致

### 幻读（Phantom Read）

当前事务读取相同数据2次的过程期间，另一个事务**插入**了新行、以此造成当前事务读取的2次结果不一致。

<aside>
💡 The so-called phantom problem occurs within a transaction when the same query produces different sets of rows at different times

</aside>

**看上不可重复读与幻读两者差不多，但不可重复读偏向于行的修改和删除，幻读偏向于行的新增**

> **在RR的隔离级别下，Innodb使用MVVC和next-key locks解决幻读，MVVC解决的是普通读（快照读）的幻读，next-key locks解决的是当前读情况下的幻读**。RR存在幻读的，InnoDB是非标准的RR
> 

# 其他

**一致的非锁定读取**

使用REPEATABLE READ 隔离级别时，快照基于执行第一次读取操作的时间。

使用READ COMMITTED隔离级别，快照将重置为每个一致的读取操作的时间。

```sql
# 获取当前事务隔离界别，默认RR可重复读
SHOW VARIABLES LIKE '%transaction_isolation%';

# 设置当前会话事务隔离级别为“读未提交”
SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;
```

# 附录

[MySQL :: MySQL 5.7 Reference Manual :: 14.7.2.1 Transaction Isolation Levels](https://dev.mysql.com/doc/refman/5.7/en/innodb-transaction-isolation-levels.html)