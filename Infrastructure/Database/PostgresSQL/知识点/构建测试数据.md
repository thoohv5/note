---
title: 构建测试数据
date: 2026-04-07
tags: [基础设施, 数据库]
type: note
status: complete
---

# 构建测试数据

```sql
\df generate*
```

### 顺序值

```sql
select id from generate_series(1,10) t(id);
```

### 随机值

```sql
--10内的随机整数
select (random()*10)::int from generate_series(1,10);
--100内的随机整数
select (random()*100)::int from generate_series(1,10);
--100内的随机两位小数
select (random()*100)::real(4,2) from generate_series(1,10);
```

### 生成时间

```sql
--间隔5秒
select generate_series(to_date('20220314','yyyymmdd'),to_date('20220315','yyyymmdd'),'5s');
--间隔5分
select generate_series(to_date('20220314','yyyymmdd'),to_date('20220315','yyyymmdd'),'5m');
--间隔5小时
select generate_series(to_date('20220314','yyyymmdd'),to_date('20220315','yyyymmdd'),'5h');
--间隔5天
select generate_series(to_date('20220214','yyyymmdd'),to_date('20220315','yyyymmdd'),'5d');
--间隔5个月
select generate_series(to_date('20200314','yyyymmdd'),to_date('20220315','yyyymmdd'),'5mon');
--间隔5年
select generate_series(to_date('20000314','yyyymmdd'),to_date('20220315','yyyymmdd'),'5y');
```

### 随机字符串

```sql
--随机生成32位md5字符串10行
select md5(random()::text) from generate_series(1,10);  
--随机生成2个32位md5字符串连接值
select repeat(md5(random()::text),2);
--随机生成2个32位md5字符串连接值10行
select repeat(md5(random()::text),2) from generate_series(1,10);
--指定字符串10次以内的随机重复次数
select repeat('abcd',(random()*10)::integer);
```

### 拼接字符串

```sql
select concat('ab','cd');
select concat('ab',' ','cb');  
select concat('ab',' ','cb',' ','ef');
```

### 随机整数

```sql
create or replace function get_rand_int (m int ,n int) returns int as $body$
begin
    return cast((random() *(n - m) + m) as int);
end ;
$body$
language plpgsql;
```

### 随机中文

```sql
create or replace function get_ch(int) returns text as $$    
declare    
  res text;    
begin    
  if $1 >=1 then    
    select string_agg(chr(19968+(random()*20901)::int), '') into res from generate_series(1,$1);    
    return res;    
  end if;    
  return null;    
end;    
$$ language plpgsql strict;
```

### 随机身份证号码

```sql
create or replace function gen_id(    
  a date,    
  b date    
)     
returns text as $$    
select lpad((random()*99)::int::text, 2, '0') ||     
       lpad((random()*99)::int::text, 2, '0') ||     
       lpad((random()*99)::int::text, 2, '0') ||     
       to_char(a + (random()*(b-a))::int, 'yyyymmdd') ||     
       lpad((random()*99)::int::text, 2, '0') ||     
       random()::int ||     
       (case when random()*10 >9 then 'X' else (random()*9)::int::text end ) ;    
$$ language sql strict;
```

### 随机电话号码

```sql
create or replace function get_tel() returns varchar(300) as $body$
declare
        startlength int  default 11 ;
        endlength int  default 11  ;
        first_no varchar(100) default '1';
        chars_str varchar(100) default '0123456789';
        return_str varchar(300) default substring('3578' , cast((1 + random()*3 ) as int),1);
        i int ;
        end1 int;
    begin
        end1 :=cast((random()*(endlength - startlength)) as int)+startlength;
        for i in 1 .. end1-2 loop
        return_str = concat(return_str,substring(chars_str , cast((1 + random()*9 ) as int),1));
        end loop;
        return concat(first_no,return_str);
    end;
$body$
language 'plpgsql' ;
```

### 随机日期

```sql
create or replace function get_rand_date(start_date date,end_date date) returns date as $body$   
declare
        interval_days integer ;
        random_days integer ;
        random_date date ;
    begin
        interval_days := end_date - start_date ;
        random_days := get_rand_int (0, interval_days) ;
        random_date := start_date + random_days ;
        return random_date;
 end ;
$body$
language plpgsql;
```

### 随机时间

```sql
create or replace function get_rand_datetime(start_date date, end_date date) returns timestamp as $body$  
declare 
        interval_days integer;
        random_seconds integer; 
        random_dates integer;
        random_date date; 
        random_time time;
    begin
        interval_days := end_date - start_date;
        random_dates:= trunc(random()*interval_days);
        random_date := start_date + random_dates;
        random_seconds:= trunc(random()*3600*24);
        random_time:=' 00:00:00'::time+(random_seconds || ' second')::interval;
        return random_date +random_time;
    end; 
$body$ 
language plpgsql;
```

### IP

```sql
create or replace function get_ip() returns inet as $body$
begin
return concat(generate_series(0, 255), '.', generate_series(0, 255), '.', generate_series(0, 255), '.', generate_series(0, 255))::inet;
end ;
$body$
language plpgsql;
```

### net

```sql
create or replace function get_network() returns cidr as $body$
declare 
mask int;
params int[];
cur int;
dm int;
begin
mask := (random()*10)::int;
dm := mask;
for i in 0 .. 3 loop
	if dm < 8 then
		cur := dm;
		dm := 0;
	else
		cur := 8;
		dm := dm - cur;
	end if;
	
	if cur = 0 then
		params[i] = 0;
	else
		params[i] = power(2, cur)-1;
	end if;
end loop;
return concat((random()*params[0])::int, '.', (random()*params[1])::int, '.', (random()*params[2])::int, '.', (random()*params[3])::int, '/', mask)::inet;
end ;
$body$
language plpgsql;
```

### MAC

```sql
create or replace function get_mac() returns macaddr as $body$
begin
return concat(to_hex((random()*255)::int), '-', to_hex((random()*255)::int), '-', to_hex((random()*255)::int), '-', to_hex((random()*255)::int), '-', to_hex((random()*255)::int), '-', to_hex((random()*255)::int))::macaddr;
end ;
$body$
language plpgsql;
```

```sql
insert into test(ip, mac, content) select get_ip(),get_mac(),concat(get_zh(200), get_ip(),get_mac()) from generate_series(1,600);
```

```sql
select id, generate_series(0, split_part(id, '-', 2)::inet-split_part(id, '-', 1)::inet)+split_part(id, '-', 1)::inet - '0.0.0.0'::inet as ip, type, regexp_replace(regexp_replace(config, '.*"hardware_ethernet"=>"', ''), '", "failover_name".*', '') as hardware_ethernet from dhcpresource limit 10;
```

```sql
insert into usageinfo(ip_addr, type, ip_resource_id, lease_state, hardware_ethernet) select generate_series(0, split_part(id, '-', 2)::inet-split_part(id, '-', 1)::inet)+split_part(id, '-', 1)::inet - '0.0.0.0'::inet, type, id, 'free', regexp_replace(regexp_replace(config, '.*"hardware_ethernet"=>"', ''), '", "failover_name".*', '')::text from dhcpresource order by case type when 'static' then 0 when 'dynamic' then 1 end ON CONFLICT (ip_addr) DO NOTHING;
```

```sql
insert into usageinfo(ip_addr, type, ip_resource_id, lease_state, hardware_ethernet) select generate_series(0, split_part(id, '-', 2)::inet-split_part(id, '-', 1)::inet)+split_part(id, '-', 1)::inet - '0.0.0.0'::inet, type, id, 'free', regexp_replace(regexp_replace(config, '.*"hardware_ethernet"=>"', ''), '", "failover_name".*', '')::text from dhcpresource where type = 'dynamic' order by case type when 'static' then 0 when 'dynamic' then 1 end ON CONFLICT (ip_addr) DO NOTHING;
```