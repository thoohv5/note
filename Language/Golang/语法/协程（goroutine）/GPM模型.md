---
title: GPM模型
date: 2026-04-08
tags: [编程语言, Golang]
type: guide
status: complete
---

# GPM模型

# 概述

并发：逻辑上具有处理多个同时性任务的能力

并行：物理上同一时刻执行多个并发任务

[[../../../知识点/进程 & 线程 & 协程]] 

# **线程模型（用户空间线程与内核空间线程比例模型）**

- N:1模型，N个用户空间线程在1个内核空间线程上运行。优势是上下文切换非常快但是无法利用多核系统的优点。
- 1:1模型，1个内核空间线程运行一个用户空间线程。这种充分利用了多核系统的优势但是上下文切换非常慢，因为每一次调度都会在用户态和内核态之间切换。（POSIX线程模型(pthread)，Java）
- M:N模型， 每个用户线程对应多个内核空间线程，同时也可以一个内核空间线程对应多个用户空间线程。Go打算采用这种模型，使用任意个内核模型管理任意个goroutine。这样结合了以上两种模型的优点，但缺点就是调度的复杂性。

**缺点**

1. 高消耗（占用内存多）
2. 高切换（频繁切换）

# GMP**模型**

![](https://raw.githubusercontent.com/thoohv5/ob/main/picture20231124183706.png)

GMP模型中

**G，Goroutine**
被调度的实体，即**用户空间线程（协程）**，在本地队列中不断的切换执行

**M，Thread**
传统线程实体，即**内核空间线程**，负责代码的执行

- M 的个数和 P 的个数不一定一样多（会有休眠的M 或 P不绑定M ）（最大10000）

**P，Processor**
处理器抽象，又称上下文，目的是实现串联G的本地队列，当**M持有P**时，访问的G不会出现数据竞争

- P 的个数就是`GOMAXPROCS`（最大256），启动时固定的，一般不修改
- P 是用一个全局数组（255）来保存的，并且维护着一个全局的 **P 空闲链表**

**Global队列**

存放等待运行的G

**Local队列**
存放等待运行的G

- 数量限制（256）
- 优先将创建的G放入对应P的Local队列中，如果满了放入Global队列

## 设计策略

程序创建的goroutine会被调度器放到本地运行队列（局部性，优先）或全局运行队列中，之后调度器会将Global queue中的goroutine分配给一个逻辑处理器，并放到这个逻辑处理器的本地运行队列中。本地运行队列中的goroutine会一直等到自己被分配的逻辑处理器执行，我们一般将 GOMAXPROCS 的个数设置为 CPU 的核数，且需要注意的是：

- go 1.5 版本之前的 GOMAXPROCS 默认是 1
- go 1.5 版本之后的 GOMAXPROCS 默认是 Num of cpu

### 复用线程

**work stealing机制**

WSS：工作窃取调度

1. M切换出G后，将其插入本地队列尾部
2. **本地队列为空则去全局队列偷**
3. 然后才去其他P本地队列偷
4. 没有工作则休眠

**hand off机制**

![](https://raw.githubusercontent.com/thoohv5/ob/main/picture20231124182945.png)

![](https://raw.githubusercontent.com/thoohv5/ob/main/picture20231124183244.png)

### 利用并行

`GOMAXPROCS`

### 抢占式调度

在coroutine中要等待一个协程主动让出CPU才执行下一个协程，在Go中，一个goroutine:最多占用CPU1Oms,防止其他gofoutine:被饿死，

在 **Go 1.1** 版本中，调度器还不支持抢占式调度，只能依靠 goroutine 主动让出 CPU 资源，存在非常严重的调度问题：

- 单独的 goroutine 可以一直占用线程运行，不会切换到其他的 goroutine，造成饥饿问题
- 垃圾回收需要暂停整个程序（Stop-the-world，STW），如果没有抢占可能需要等待几分钟的时间，导致整个程序无法工作

在**Go 1.12**版本中编译器在特定时机插入函数，通过函数调用作为入口触发抢占，实现了协作式的抢占式调度。

**同步协作式调度**

```go
runtime.GOMAXPROCS(1)
runtime.Gosched()

```

原理：

go程序启动时会首先创建一个特殊的内核线程 `sysmon`，用来监控和管理，其内部是一个循环：

1. 记录所有 P 的 G 任务的`计数 schedtick`，schedtick会在每执行一个G任务后递增
2. 如果检查到 `schedtick` 一直没有递增，说明这个 P 一直在执行同一个 G 任务，如果超过**10ms**，就在这个G任务的栈信息里面加一个 tag 标记
3. 然后这个 G 任务在执行的时候，如果遇到非内联函数调用，就会检查一次这个标记，然后中断自己，把自己加到队列末尾，执行下一个G
4. 如果没有遇到`非内联函数` 调用的话，那就会一直执行这个G任务，直到它自己结束；如果是个死循环，并且 GOMAXPROCS=1 的话。那么一直只会只有一个 P 与一个 M，且队列中的其他 G 不会被执行

[[内联函数]] 

Go 1.14 **异步抢占式调度**（基于系统信号）

原理：首先注册绑定 SIGURG 信号及处理方法runtime.doSigPreempt，sysmon会间隔性检测超时的p，然后发送信号，m收到信号后休眠执行的goroutine并且进行重新调度。

### Global队列

## 生命周期

### M0&G0

go中还有特殊的M和G, 它们是M0和G0。

**M0（进程唯一）**

- 启动程序后的编号为0的主线程
- 在全局变量runtime.m0中，不需要在heap上分配
- M0负责执行初始化操作和启动第一个G, 在之后M0就和其他的M一样

**G0（线程唯一）**

- 每次启动一个M,都会第一个创建的gourtine,就是G0
- G0是仅用于负责调度的G,，G0不指向任何可执行的函数
- 每个M都会有一个自己的G0，在调度或系统调用时会使用G0的栈空间，全局变量的G0是M0的G0

# 附录

[深入golang runtime的调度](https://zboya.github.io/post/go_scheduler/)

[2、Golang的协程调度器原理及GMP设计思想 · 语雀](https://www.yuque.com/aceld/golang/srxd6d)