---
title: golang 垃圾回收
date: 2026-04-07
tags:
  - 其他
type: guide
status: complete
---

## golang 垃圾回收

踩内存，内存泄漏，野指针

踩内存：访问了不应该访问的内存地址

内存泄漏：动态申请的内存空间没有正常释放，但是也不能正确使用的情况。

野指针（等于悬挂指针）：指向被释放或者访问受限内存的指针

golang 的垃圾回收基于的理论是三色标记法，并且通过合理的使用内存屏障技术，把垃圾回收的 stw 几乎消灭

GC：垃圾回收管理内存的方式

RC：引用计数管理内存的方式

三色指的是白色，灰色，黑色

栈内存由编译器来管理，堆内存由程序员来管理

逃逸分析

从根处扫描，把所有的根扫描完，每个根扫描到底。按照之前的三色标记来说，扫描完了的是黑色，正在扫描的是灰色的，没扫描的是白色的。根扫描完了，那么最后只会剩下两种颜色的，黑色，白色。

内存屏障

插入写屏障

混合写屏障（插入写屏障 + 删除写屏障）

1. 内存屏障只是对应一段特殊的代码
2. 内存屏障这段代码在**编译期间**生成
3. 内存屏障本质上在**运行期间**拦截内存写操作，相当于一个 hook 调用

golang ABI（Application Binary Interface）标准就是这样的，传参数用栈，返回值也用栈

我们再理解下最本质的东西，触发了写屏障之后，我们的核心目的是为了能够把赋值的前后两个值记录下来，以便 GC 垃圾回收器能得到通知，从而避免错误的回收。记录下来是最本质的，但是并不是要立马处理，所以这里做的优化就是，攒满一个 buffer ，然后批量处理，这样效率会非常高的。

golang 的垃圾回收是并发垃圾回收设计，业务运行和回收器运行并发

如果你要安全的实现回收垃圾，那么简单的就是回收垃圾的时候，把所有的业务操作都停止，这个术语是STW（stop the world）

STW 是全局的赋值器挂起，我们一直说 golang 消除了 STW 说的是没有了全局性的挂起，但是局部的赋值器挂起是一直有的，包括现在也是有的。

Dijkstra 算法

删除写屏障：也叫做基于其实快照的解决方案（snapshot-at-the-begining）