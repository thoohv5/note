# pidof

```bash
pidof命令可以快速的找到应用程序的pid进程号

 
pidof命令

-s 返回多进程，返回一个PID
-c 只返回进程ID
-x 重复命中，返回指定程序的 shell脚本的 pid
-o <进程号>  
      告诉 pidof 忽略带有进程id的进程。 这个特定的pid %PPID能够被用对
      pidof的程序的父亲进程来命名，换句话说就是调用shell或shell脚本.
```