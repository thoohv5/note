---
title: 磁盘分区
date: 2026-04-07
tags: [基础设施, Linux, General]
type: reference
status: complete
---

## 磁盘分区

## 概述

分区就是根据用户不同的使用需求划分出不同容量的空间，然后，对划分出的空间进行重新命名操作。作用就是方便使用者对数据归类、方便查阅及预览。

## 分类

磁盘分区：主磁盘分区，扩展磁盘分区，逻辑分区

### 主分区

作用启动操作系统的，主要放的是操作系统的启动或引导程序MBDR分区中，最多有4个主分区

### 扩展分区

扩展分区是不能直接使用的，以逻辑分区的方式来使用，扩展分区可分成若干逻辑分区。逻辑分区都是扩展分区的一部分。

### 逻辑分区

逻辑分区必须构建在扩展分区上，而不是构建在主分区上，可构建多个

## 联系

磁盘的容量=主分区容量+扩展分区容量
扩展分区容量=各个逻辑分区的容量之和

MBR（主引导记录）的分区表（主分区表）只能存放4个分区，如果要分更多的分区的话就要一个扩展分区表（EBR），扩展分区表放在一个系统ID为0x05的主分区上，这个主分区就是扩展分区，扩展分区可以分若干个分区，每个分区都是一个逻辑分区。