# 收尾函数

### `LAG()` / `LEAD()`：前一行或后一行的值

### 

```sql
SELECT
  employee_id, salary,
  LAG(salary) OVER (ORDER BY hire_date) AS prev_salary,
  LEAD(salary) OVER (ORDER BY hire_date) AS next_salary
FROM employees;
```