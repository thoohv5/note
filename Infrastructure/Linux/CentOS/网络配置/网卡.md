---
title: 网卡
date: 2026-04-07
  - 基础设施
  - Linux
type: note
status: complete
---

## 网卡

## 命令

```bash
## 启动网卡
ifconfig ens192 up
## 关闭网卡
ifconfig ens192 down

## 需要依赖 /etc/sysconfig/network-scripts 文件中的网卡名称
## 启动网卡
ifup ens192
## 关闭网卡
ifdowm ens192
```

ifup 与 ifdown 真是太简单了。这两个程序其实是 script 而已，它会直接到 /etc/ sysconfig/network-scripts 目录下搜索对应的配置文件，例如 ifup eth0，它会找出 ifcfg-eth0 这个文件的内容，然后加以设置。关于 ifcfg-eth0 的设置请参考下面的说明。
　　不过，由于这两个程序主要是搜索设置文件（ifcfg-ethx）来进行启动与关闭的，所以在使用前请确定 ifcfg-ethx 是否真的存在于正确的目录内，否则会启动失败。另外，如果以 ifconfig eth0 来设置或者是修改了网络接口后，就无法再以 ifdown eth0 的方式来关闭了。因为 ifdown 会分析比较目前的网络参数与 ifcfg-eth0 是否相符，不符的话，就会放弃这次操作。因此，使用 ifconfig 修改完毕后，应该要以 ifconfig eth0 down 才能够关闭该接口。

## 文件

```bash
cd /etc/sysconfig/network-scripts
vim ifcfg-ens160

---------------------------------------------
/etc/sysconfig/network-scripts/ifcfg-ensxxx

TYPE="Ethernet"
PROXY_METHOD="none"
BROWSER_ONLY="no"
BOOTPROTO="none"
DEFROUTE="yes"
IPV4_FAILURE_FATAL="no"
IPV6INIT="yes"
IPV6_AUTOCONF="no"
IPV6_DEFROUTE="yes"
IPV6_FAILURE_FATAL="no"
NAME="ens160"
UUID="ac0ed56b-6110-4944-8d6d-782da7ed02b1"
DEVICE="ens160"
ONBOOT="yes"

IPADDR="10.2.3.130"
PREFIX="22"
GATEWAY="10.2.0.1"
DNS1="114.114.114.114"

IPV6ADDR=2001:3:130/64
IPV6_DEFAULTGW=2001::1
-----------------------------------------------

## 修改ONBOOT
ONBOOT="no"

## 重启网络
systemctl restart network
```

## **重启网卡提示Bringing up interface eth0: Device eth0 does not seem to be present,delaying initialization.**

```bash
## 因为克隆的机器没有正确的mac,UUID信息冲突导致的
vim /etc/udev/rules.d/70-persistent-net.rules
```