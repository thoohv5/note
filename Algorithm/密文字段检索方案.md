---
title: 密文字段检索方案
date: 2026-04-07
tags: [算法]
type: note
status: complete
---

# 密文字段检索方案

密文检索的功能实现是根据4位英文字符（半角），2个中文字符（全角）为一个检索条件。将一个字段拆分为多个，

比如：

**taobao123使用4个字符为一组的加密方式。**

**第一组 taob ，第二组aoba ，第三组obao ，第四组 bao1 … 依次类推**

**如果需要检索 所有包含 检索条件4个字符的数据 比如：aoba ，加密字符后通过key like “%partial%” 查库。** 

因为密文检索开启后 密文长度会膨胀几倍以上，如果没有强需求建议不开启。

但是使用这种方式也有一定代价：

- 支持模糊查询加密方式，产出的密文比较长；
- 支持的模糊查询。不支持过短的查询(出于安全考虑)；
    
    子句长度必须大于等于4个英文/数字，或者2个汉字
    
- 返回的结果列表中有：对记录先解密，再筛选；
    
    可能有多余的结果，需要增加筛选的逻辑
    

本产品允许对每一个字段都独立设置这个字段的加密模式。**请您根据自己的应用场景确认每个字段的加密方案。请您根据您的业务仔细审查、选择。一旦加密开始之后，再更改成本就较高了。**

# 附录

[阿里安全开放平台-阿里聚安全](https://jaq-doc.alibaba.com/docs/doc.htm?treeId=1&articleId=106213&docType=1)

[mp.weixin.qq.com](https://mp.weixin.qq.com/s/SmHimTQINvsPE_hM843ytQ)